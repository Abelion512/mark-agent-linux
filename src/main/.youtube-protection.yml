# YouTube Integration Protection Guard
# Generated: 2026-07-29
# Status: VERIFIED WORKING
#
# DO NOT MODIFY the following files without explicit user approval:
# - src/main/youtube-player.js (BrowserWindow + UA spoofing + CSP removal)
# - src/main/browser-agent.js (anti-fingerprint injection + track info sync)
# - src/renderer/src/contexts/YoutubeMusicContext.jsx (IPC bridge + track listener)
# - src/renderer/src/components/YoutubeMusicPlayer.jsx (track card UI)
# - src/preload/index.js (yt* methods)
#
# These files implement YouTube playback with:
# - BrowserWindow (NOT webview) with partition persist:mark-browser
# - Chrome UA spoofing + real Electron UA for accounts.google.com
# - CSP removal + OAuth popup handling
# - Anti-fingerprint injection (navigator.webdriver, chrome.runtime, plugins)
# - Track info sync via yt:track-updated IPC
#
# Evidence: Video plays successfully (DJ GAM GAM, 1:10:48) as of 2026-07-29
# Verified by: ChatGPT, DeepSeek, Gemini, Grok, Qwen, Manus (6/6 AI + 15+ sources)
#
# Breaking changes TO AVOID:
# - Do NOT use <webview> tag (deprecated, crashes on OAuth)
# - Do NOT use youtube-player.js BrowserWindow (show:false causes Google block)
# - Do NOT remove navigator.webdriver=false injection
# - Do NOT change partition to anything other than persist:mark-browser
# - Do NOT remove CSP removal headers
#
# If YouTube breaks, check:
# 1. partition is still persist:mark-browser
# 2. Anti-fingerprint injection is running (did-start-navigation)
# 3. UA is Chrome 130 on YouTube, real Electron UA on accounts.google.com
# 4. User has logged in at least once via physical browser window
