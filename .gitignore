# deps
/node_modules/
/bun.lock.bak

# build output
dist/
out/
build/

# rust
**/*.rs.bk

# env & secrets
.env
.env.*
*.pem
*.key
.env.local
.env.development.local
.env.test.local
.env.production.local

# logs & cache
*.log*
.eslintcache
.cache/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# coverage
coverage/

# editor / OS
.vscode/
.idea/
.DS_Store
*.swp

# worktree lokal & cache lain
/.worktrees/
/worktrees/
/.claude/
__pycache__/

# file lokal tool AI (plan/state per-tool, bukan bagian repo)
/.hermes/
/.zcode/
/.impeccable/
/.remember/
/.superpowers/
/plans/

# project-specific
workflow
mark_chrome_profile
web.whatsapp.com.html
/web.whatsapp.com_files
PLUGIN_SYSTEM_SRS.md
.mark/
social-data-engine/
scripts/tmp-*
# NOTE: .githooks/ di-track (aturan lama dihapus 2026-09-03 karena
# bertentangan dengan isi repo; aturan direktori juga memblokir `git add`).

# artefak generator — regenerasi on-demand, jangan di-commit (audit 2026-08-26)
upstream-diff-report.json


# scratch verifikasi agent (baseline lint), jangan commit
.lint-baseline/

# docs/PLANNED - internal planning docs, not for open-source
/docs/PLANNED/
/docs/superpowers/
docs/DRIFT_MANIFEST.md
docs/.screenshoots/
PARITY-AUDIT-FINAL.md

# tests - stability/ di-ignore; stress-harness justru DI-TRACK (dijalankan
# via `bun run test:harness` & CI), aturan lama dihapus 2026-09-03.
/tests/stability/

# scripts - hanya helper lokal yang benar-benar tidak di-commit
/scripts/ask-ais.mjs
/scripts/rsi-*.mjs
/scripts/import-tiktok-cookies.mjs
# NOTE: auto-detect-upstream / mark-update / build-manifest / bump-version /
# sync-version DI-TRACK (dipakai release automation & CI) — aturan ignore
# lamanya dihapus 2026-09-03 karena bertentangan dan membingungkan agent.

# src-tauri - DI-TRACK penuh di repo ini (Tauri port). Aturan `/src-tauri/`
# dihapus 2026-09-03: pola direktori memblokir `git add <file-tracked>` di
# bawahnya, yang membuat Release Prepare (git add src-tauri/*) gagal.
# Target Cargo & gen/schemas tetap di-ignore oleh src-tauri/.gitignore.

# HTML tracker files - generated artifacts, jangan di-git
/project-tracker.html
/mark-agent-tracker.html

# what's-new.json DI-TRACK (dibangkitkan release automation) — aturan
# ignore lama dihapus 2026-09-03 karena bertentangan.

# Agent-CLI local config (Codex, dst.)
.codex/
