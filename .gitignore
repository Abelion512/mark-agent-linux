# ── Dependencies ──
node_modules/
.pnpm-store/
.npm/

# ── Lockfiles (use npm only) ──
yarn.lock
yarn-error.log
pnpm-lock.yaml

# ── Build output ──
dist/
out/
dist-electron/
build/
release/
*.asar
*.asar.unpacked

# ── Electron packaging ──
*.AppImage
*.deb
*.snap
*.rpm
*.flatpak
*.dmg
*.exe
*.msi
*.nupkg
RELEASES
electron-builder.yaml.local

# ── IDE / Editor ──
.vscode/
.idea/
*.swp
*.swo
*~
*.orig

# ── OS artifacts ──
.DS_Store
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# ── Environment / secrets ──
.env
.env.*
!.env.example
*.pem
*.cert
*.key
*.bak

# ── Logs ──
*.log
npm-debug.log*
yarn-debug.log*
debug.log
error.log
lerna-debug.log*

# ── Cache & temp ──
.eslintcache
.vite/
.cache/
*.tsbuildinfo
.turbo/

# ── Coverage / test ──
coverage/
.nyc_output/
__pycache__/
*.pyc

# ── Runtime artifacts ──
mark_chrome_profile/
wa-auth/
web.whatsapp.com.html
web.whatsapp.com_files/
*.db
*.sqlite
*.sqlite3

# ── AI agent artifacts ──
CLAUDE.md
AGENTS.md.local
.local/
.hermes/
.superpowers/
.zcode/
quest.md
docs/PLANNED/
docs/superpowers/
docs/.screenshoots/
.remember/agent-memory/
exa-results/
.claude/
!.claude/settings.json
!.claude/settings.local.json

# ── Local config ──
model-registry.json
9router-config.json
credentials.json
service-account.json
client_secret*.json
*-config.local.*

# ── Generated / large test artifacts ──
tests/**/*.har
tests/**/*.heapsnapshot
tests/**/*.gz

# ── Node.js version specifiers (workspace-specific, not project) ──
.nvmrc
.node-version
.nodenv-version

# ── Pluggable skill cache ──
agent-skills-cache.json
