# ── Dependencies ──
node_modules/
.pnpm-store/
.npm/

# ── Lockfiles (use npm only) ──
yarn.lock
yarn-error.log
pnpm-lock.yaml

# ── Build output ──
dist/
out/
dist-electron/
build/
release/
*.asar
*.asar.unpacked

# ── Electron packaging ──
*.AppImage
*.deb
*.snap
*.rpm
*.flatpak
*.dmg
*.exe
*.msi
*.nupkg
RELEASES
electron-builder.yaml.local

# ── IDE / Editor ──
.vscode/
.idea/
*.swp
*.swo
*~
*.orig

# ── OS artifacts ──
.DS_Store
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# ── Environment ──
.env
.env.example
.env.local
.env.*.local
*.pem
*.cert
*.key

# ── Logs ──
*.log
npm-debug.log*
yarn-debug.log*
debug.log
error.log
lerna-debug.log*

# ── Cache & temp ──
.eslintcache
.vite/
.cache/
*.tsbuildinfo
.turbo/

# ── Coverage / test ──
coverage/
.nyc_output/
__pycache__/
*.pyc

# ── Runtime artifacts ──
mark_chrome_profile/
wa-auth/
web.whatsapp.com.html
web.whatsapp.com_files/
*.db
*.sqlite
*.sqlite3

# ── AI agent artifacts (local overrides) ──
CLAUDE.md
AGENTS.md.local
.local/
.hermes/
.superpowers/
.zcode/
quest.md
docs/PLANNED/
docs/superpowers/

# ── Local config ──
model-registry.json
9router-config.json
credentials.json
service-account.json
client_secret*.json
*-config.local.*

# ── Native addon builds ──
*.node
build/Release/

# ── Performance test artifacts (large, generated) ──
tests/*.har
tests/*.heapsnapshot
tests/*.gz
.env.bak
